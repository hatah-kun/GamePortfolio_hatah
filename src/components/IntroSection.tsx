const IntroSection = () => {
  return (
    <div
      id="home_sec"
      className={`h-screen w-full flex justify-center items-center
        bg-gradient-to-b from-customColor-primary to-customColor-secondary`}
    ></div>
  );
};

export default IntroSection;
